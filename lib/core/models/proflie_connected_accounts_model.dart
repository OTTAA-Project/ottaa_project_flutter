class ProfileConnectedAccounts {
  final String name, imageUrl, id;
  bool isExpanded;

  ProfileConnectedAccounts({
    required this.name,
    required this.imageUrl,
    required this.id,
    required this.isExpanded,
  });
}
