import 'package:flutter/material.dart';

class VoiceAndSubtitleScreen extends StatelessWidget {
  const VoiceAndSubtitleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}