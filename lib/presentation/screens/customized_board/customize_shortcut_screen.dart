import 'package:flutter/material.dart';

class CustomizeShortcutScreen extends StatelessWidget {
  const CustomizeShortcutScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        height: 300,
        width: 200,
        color: Colors.pink,
      ),
    );
  }
}
